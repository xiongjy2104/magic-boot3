
server:
  port: 8089

spring:
  datasource:
    username: root
    password: 123456

upload:
  dir: /opt/upload/


# 框架配置
magic-api:
  show-sql: true #配置打印SQL
  resource: # 配置存储方式，默认使用的是文件
    location: data/magic-api # 接口信息存放外部指定目录

  security:
    username: admin
    password: 123456